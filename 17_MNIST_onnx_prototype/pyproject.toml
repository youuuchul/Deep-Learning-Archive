[project]
name = "mnist-onnx-prototype"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "streamlit",
    "streamlit-drawable-canvas",
    "onnxruntime",
    "numpy",
    "pillow",
    "pandas",
    "plotly",
]

[tool.ruff]
line-length = 88
